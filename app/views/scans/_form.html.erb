<%= form_with(model: scan, local: true) do |form| %>
  <% if scan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(scan.errors.count, "error") %> prohibited this scan from being saved:</h2>

      <ul>
      <% scan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.text_field :product_id, id: :scan_product_id %>
  </div>

  <div class="field">
    <%= form.label :checkout_id %>
    <%= form.text_field :checkout_id, id: :scan_checkout_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
